# Skill Writing Guide

Every generated `SKILL.md` should begin with YAML frontmatter containing `name` and `description`.

Keep each skill focused on one job. Write imperative workflow steps, define required inputs and outputs, move bulky detail into `references/`, and add scripts only for deterministic repeated work.
