# QA Agent Guide

Use QA roles for cross-boundary verification.

Good QA compares contracts across boundaries: API shape vs UI assumptions, spec vs implementation, test output vs claimed behavior, manifest paths vs packaged files.

Findings should include evidence, affected artifact, severity, and next action.
